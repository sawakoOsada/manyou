<%= form_with(model: @user, local: true, url: admin_user_path(@user)) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit '送信する' %>
<% end %>
