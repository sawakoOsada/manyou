<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>
<%= link_to 'taskを作成する', new_task_path %>
